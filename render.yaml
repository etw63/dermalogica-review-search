services:
  - type: web
    name: dermalogica-review-search
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn vector_search_app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
